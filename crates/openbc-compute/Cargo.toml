[package]
name = "openbc-compute"
version = "0.1.0"
edition.workspace = true

[features]
default = []
gpu = ["dep:wgpu"]

[dependencies]
rayon.workspace = true
sha2 = "0.10"
thiserror.workspace = true
wgpu = { workspace = true, optional = true }
